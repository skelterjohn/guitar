server {
    listen 8080;
    server_name _;
    root /usr/share/nginx/html;
    index catalog.html;

    # Cloud Run terminates TLS and forwards to :8080; keep redirects relative
    # so browsers stay on https://guitar.skelterjohn.me/…
    absolute_redirect off;

    gzip on;
    gzip_types text/css application/javascript application/json;

    location ~ ^/(sw\.js|sw-rep\.js|workbox-.*\.js|manifest\.webmanifest|rep\.webmanifest)$ {
        try_files $uri =404;
        add_header Cache-Control "no-cache";
    }

    location /assets/ {
        try_files $uri =404;
        add_header Cache-Control "public, max-age=31536000, immutable";
    }

    location ~ \.mjs$ {
        types { }
        default_type application/javascript;
        try_files $uri =404;
    }

    location = / {
        return 301 /catalog;
    }

    location = /catalog {
        try_files /catalog.html =404;
    }

    location ^~ /catalog/ {
        try_files $uri $uri/ /catalog.html;
    }

    location = /rep {
        add_header X-Robots-Tag "noindex, nofollow" always;
        try_files /rep.html =404;
    }

    location ^~ /rep/ {
        add_header X-Robots-Tag "noindex, nofollow" always;
        try_files $uri $uri/ /rep.html;
    }

    location ^~ /view/ {
        return 301 /catalog$request_uri;
    }

    location / {
        try_files $uri $uri/ /catalog.html;
    }

    location /pdf/ {
        proxy_pass https://storage.googleapis.com/skelterjohnguitar-pdf/;
        proxy_ssl_server_name on;
        proxy_set_header Host storage.googleapis.com;
    }
}
